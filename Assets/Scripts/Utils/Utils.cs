namespace Utils
{
    public enum GameStates : byte
    {
        Joining,
        Playing,
        Paused,
        GameOver,
    }

    public enum CameraTypes
    {
        FreeLook,
        Locked 
    }
}
